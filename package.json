{
  "name": "torch-cli",
  "version": "1.0.0",
  "description": "Context aware, LLM-powered CLI for the 10x dev 🏎️",
  "main": "index.js",
  "scripts": {
    "build": "tsc && npm link",
    "dev": "tsc && npm link && torch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.8.0",
    "@types/prompt-sync": "^4.2.1"
  },
  "bin": {
    "torch": "./dist/index.js"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "ignore": "^5.2.4",
    "langchain": "^0.0.156",
    "prompt-sync": "^4.2.0"
  }
}
